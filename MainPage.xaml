<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TvBoxApp.MainPage"
             BackgroundColor="#121212">

    <VerticalStackLayout Padding="40" Spacing="40">

        <!-- TÍTULO -->
        <Label Text="Gcoder - Guilherme Lopes Apps"
               FontSize="36"
               TextColor="White"
               HorizontalOptions="Center"/>

        <!-- CARDS -->
        <HorizontalStackLayout Spacing="35"
                               HorizontalOptions="Center">

            <!-- CARD CRISTAO -->
            <ContentView x:Name="CardCristao"
             WidthRequest="220"
             HeightRequest="220">

                <Border StrokeShape="RoundRectangle 20"
            BackgroundColor="#4CAF50">

                    <VerticalStackLayout VerticalOptions="Center"
                             HorizontalOptions="Center">
                        <Label Text="🙏" FontSize="60"/>
                        <Label Text="Trilha Cristã" TextColor="White"/>
                    </VerticalStackLayout>

                </Border>
            </ContentView>
            
            <!-- SORTEIO -->
            <ContentView x:Name="CardSorteio"
                         WidthRequest="220"
                         HeightRequest="220">

                <Border StrokeShape="RoundRectangle 20"
                        BackgroundColor="#00A8FF">

                    <VerticalStackLayout VerticalOptions="Center"
                                         HorizontalOptions="Center">
                        <Label Text="🎲" FontSize="60"/>
                        <Label Text="Sorteio" TextColor="White"/>
                    </VerticalStackLayout>

                </Border>
            </ContentView>

        </HorizontalStackLayout>

        <!-- CONTATO -->
        <ContentView x:Name="CardContato"
                     WidthRequest="500"
                     HeightRequest="120"
                     HorizontalOptions="Center">

            <Border StrokeShape="RoundRectangle 20"
                    BackgroundColor="#222222">

                <VerticalStackLayout VerticalOptions="Center"
                                     HorizontalOptions="Center">
                    <Label Text="Contato por Email" TextColor="White"/>
                    <Label Text="guilhermelopes_dev@hotmail.com"
                           TextColor="Gray"/>
                </VerticalStackLayout>

            </Border>
        </ContentView>

    </VerticalStackLayout>

</ContentPage>